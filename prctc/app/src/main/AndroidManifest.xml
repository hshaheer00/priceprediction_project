<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 1. Permissions required for Firebase and Network connectivity -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prctc"
        tools:targetApi="31">

        <!-- 2. Step 1: Entry Point (Splash Screen) -->
        <activity
            android:name=".splashscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 3. Step 2: User Authentication Activities -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity
            android:name=".register"
            android:exported="false" />

        <!-- 4. Step 3: Main Dashboard (Landing Page after Login) -->
        <activity
            android:name=".homepage"
            android:exported="false" />

        <!-- 5. Step 4: Specific Predictor Activities (Opened from Navbar) -->
        <activity
            android:name=".MobilePredictorActivity"
            android:exported="false" />

        <activity
            android:name=".TabletPredictorActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 6. Step 5: User Features (History/Settings) -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false" />

    </application>

</manifest>
